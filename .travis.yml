language: python

sudo: required

python:
    - "3.4"

services:
    - elasticsearch
    - mongodb
    - redis-server

addons:
  apt:
    sources:
    - mongodb-3.0-precise
    - elasticsearch-1.7
    packages:
    - mongodb-org-server
    - elasticsearch

before_script: >
    df -h
    && echo "tune elasticsearch"
    && mkdir /tmp/es-backups
    && echo '
    index.store.type: memory
    path.repo: [\"/tmp/es-backups\"]
    ' | sudo tee -a /etc/elasticsearch/elasticsearch.yml
    && cat /etc/elasticsearch/elasticsearch.yml
    && service elasticsearch restart
    && echo "tune mongodb"
    && mkdir /tmp/ramdata/
    && mount -t tmpfs -o size=512M tmpfs /tmp/ramdata/
    && sudo sed -i 's\dbPath: /var/lib/mongodb\dbPath: /tmp/ramdata/\' /etc/mongod.conf
    && cat /etc/mongod.conf
    && service mongod restart


cache:
  - pip

install:
    - pip install .
    - pip install -r requirements.txt
    - pip install nose-timer

script:
    - flake8
    - time nosetests -v --with-timer
    - time behave --format progress3 --logging-level ERROR

